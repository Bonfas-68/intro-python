# Tuples are immutable
# and it uses parenthesis not square brackets

numbers = (1,2,3)
numbers[0]
numbers[0] = 1
print(numbers[0])

# unpacking using python
coordinates =(1,2,3)
x,y,z = coordinates
print(x, y, z)